# Vendored conformance snapshots

Snapshots of `wit-java-mapping/conformance/`, vendored per mapping version:
`<mapping-ver>/<case-name>/…`.

The mapping repository is the source of truth. Never edit snapshots here
directly — refresh them from the mapping repository.
