// Copyright: the support package carries the mapping repository's license
// (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT).

package {{PACKAGE}};

/** Absent WIT type argument (empty tuple, one-sided result). */
public enum Unit { UNIT }
